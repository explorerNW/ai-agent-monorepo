# @explorernw/monitor-sdk

企业级前端监控 SDK (TypeScript 版)

本 SDK 旨在为 Web 应用提供轻量级、高可靠性的前端监控能力。核心采用**代理队列模式**与**双队列管理**机制，支持性能指标采集、全局错误捕获、API 接口监控及 SPA 路由追踪，并通过 `sendBeacon` 保证离页上报的可靠性。

## 🚀 快速开始

### 1. 安装

```bash
pnpm add @explorernw/monitor-sdk
# 或 npm install @explorernw/monitor-sdk
```
